apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: domain-tech-stack-wizard
  title: Domain-Based Tech Stack Wizard
  description: Select a domain, then choose a tech stack type, and pick options dynamically.
  tags:
    - generator
    - frontend
    - backend
    - database

spec:
  owner: user:your-username
  type: service

  parameters:
    - title: Project Stack Configuration
      type: object
      required:
        - domain
        - stackType
        - stackOption
      properties:
        domain:
          title: Select a Domain
          type: string
          enum:
            - Banking
            - Insurance
            - HealthCare
            - Education

        stackType:
          title: Select Tech Stack Type
          type: string
          enum:
            - UI Framework
            - Backend Platform
            - Database

        stackOption:
          title: Select Stack Option
          type: string

      dependencies:
        stackType:
          oneOf:
            # Banking domain options for UI Framework
            - properties:
                domain:
                  enum: [Banking]
                stackType:
                  const: UI Framework
                stackOption:
                  type: string
                  enum: [Vue.js, Next.js, Typescript]
              required:
                - stackOption
            # Banking domain options for Backend Platform
            - properties:
                domain:
                  enum: [Banking]
                stackType:
                  const: Backend Platform
                stackOption:
                  type: string
                  enum: [Java, Python, Ruby]
              required:
                - stackOption
            # Banking domain options for Database
            - properties:
                domain:
                  enum: [Banking]
                stackType:
                  const: Database
                stackOption:
                  type: string
                  enum: [MYSQL, PostgreSQL]
              required:
                - stackOption

            # Insurance domain options for UI Framework
            - properties:
                domain:
                  enum: [Insurance]
                stackType:
                  const: UI Framework
                stackOption:
                  type: string
                  enum: [React, Angular]
              required:
                - stackOption
            # Insurance domain options for Backend Platform
            - properties:
                domain:
                  enum: [Insurance]
                stackType:
                  const: Backend Platform
                stackOption:
                  type: string
                  enum: [Scala, Go, Django]
              required:
                - stackOption
            # Insurance domain options for Database
            - properties:
                domain:
                  enum: [Insurance]
                stackType:
                  const: Database
                stackOption:
                  type: string
                  enum: [MYSQL, PostgreSQL]
              required:
                - stackOption

            # HealthCare domain options for UI Framework
            - properties:
                domain:
                  enum: [HealthCare]
                stackType:
                  const: UI Framework
                stackOption:
                  type: string
                  enum: [Three.js, ReactJs, Angular 2+]
              required:
                - stackOption
            # HealthCare domain options for Backend Platform
            - properties:
                domain:
                  enum: [HealthCare]
                stackType:
                  const: Backend Platform
                stackOption:
                  type: string
                  enum: [Springboot, ASP.NET]
              required:
                - stackOption
            # HealthCare domain options for Database
            - properties:
                domain:
                  enum: [HealthCare]
                stackType:
                  const: Database
                stackOption:
                  type: string
                  enum: [MYSQL, PostgreSQL]
              required:
                - stackOption

            # Education domain options for UI Framework
            - properties:
                domain:
                  enum: [Education]
                stackType:
                  const: UI Framework
                stackOption:
                  type: string
                  enum: [Vue.js, Next.js, Typescript, Three.js, ReactJs, Angular 2+]
              required:
                - stackOption
            # Education domain options for Backend Platform
            - properties:
                domain:
                  enum: [Education]
                stackType:
                  const: Backend Platform
                stackOption:
                  type: string
                  enum:
                    - Java
                    - Python
                    - Ruby
                    - PHP Kotlin
                    - C sharp
                    - Perl
                    - Scala
                    - Go
                    - Djangi
                    - Springboot
                    - ASP.NET
              required:
                - stackOption
            # Education domain options for Database
            - properties:
                domain:
                  enum: [Education]
                stackType:
                  const: Database
                stackOption:
                  type: string
                  enum: [MYSQL, PostgreSQL]
              required:
                - stackOption

  steps:
    - id: review
      name: Review Selections
      action: pass
      input:
        message: |
          Domain: ${{ parameters.domain }}
          Tech Stack Type: ${{ parameters.stackType }}
          Selected Option: ${{ parameters.stackOption }}

  output:
    links:
      - title: Review
        url: '#'
